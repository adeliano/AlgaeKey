<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.algaekey">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".dozeActivity"></activity>
        <activity android:name=".onzeActivity" />
        <activity android:name=".dezActivity" />
        <activity android:name=".noveActivity" />
        <activity android:name=".oitoActivity" />
        <activity android:name=".seteActivity" />
        <activity android:name=".seisActivity" />
        <activity android:name=".cincoActivity" />
        <activity android:name=".quatro1Activity" />
        <activity android:name=".terceiraActivity" />
        <activity android:name=".segundaActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>